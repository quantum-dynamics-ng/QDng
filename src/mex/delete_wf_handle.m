%mexFunction to delete a wavefunction and it's handle or all wavefunction and it's handles
%input: 1) (one argument): Objecthandle -> delete a wavefunction and it's handle
%	2) (no argument): -> deletes all wavefunction and it's handles
%no output
%	function delete_wf_handle(handle)