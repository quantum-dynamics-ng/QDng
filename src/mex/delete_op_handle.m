%mexFunction to delete a Operator and it's handle or all Operators and it's handles
%input: 1) (one argument): Objecthandle -> delete a Operator and it's handle
%       2) (no argument): -> deletes all Operators and it's handles
%no output
%       function delete_wf_handle(handle)
