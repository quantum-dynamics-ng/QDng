The following software is required:
 * google protobuf
 * libxml2
 * lapack
 * blas
 * fftw3-3.2.2 (with openmp support)
 For Wavefunction compression
 * zlib (optional)
 * libbzip (optional)

Use ./genbs to generate the build system (configure) after a fresh git clone

* Matlab support
Matlab interface just needs a running qdng binary. 
 
 

